/*
Here’s how I’d set it up in your Xcode project:

RadioAppApp.swift (or YourAppNameApp.swift) → entry point, must call MainTabView().

MainTabView.swift → contains MainTabView, CurrentlyPlayingBar, BottomNavBar.

HomeView.swift → contains HomeView, StationCardView, MiniStationCard.

SearchView.swift → your SearchView code with Station.

ProfileView.swift → your ProfileView.

*.

@main
struct RadioAppApp: App {
    var body: some Scene {
        WindowGroup {
            MainTabView()
        }
    }
}


#Preview {
    MainTabView()
}
